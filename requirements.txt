# PayPilot - pinned dependencies (Python 3.11)
# Versions are pinned for reproducible installs in CI and Docker, and refreshed
# to current releases (2026-07) so `pip-audit` comes back clean.

# --- Agent orchestration ---
langgraph==1.2.7             # StateGraph that drives the 7-node recovery flow
langchain==1.3.11            # core abstractions (documents, retrievers)
langchain-openai==1.3.3      # ChatOpenAI (get_llm) + OpenAIEmbeddings (RAG)
langchain-community==0.4.2   # community integrations (FAISS vector store loader)
langchain-text-splitters==1.1.2  # RecursiveCharacterTextSplitter for playbook chunking

# --- Retrieval / RAG vector store ---
faiss-cpu==1.14.3            # in-memory FAISS index over the dunning playbook (light, fast cold start)

# --- API layer ---
fastapi==0.139.0            # POST /payment-failed, GET /health
uvicorn[standard]==0.49.0   # ASGI server for `uvicorn app.api:app`
pydantic==2.13.4            # request/response models (v2)

# --- Config ---
python-dotenv==1.2.2        # loads OPENAI_API_KEY / OPENAI_MODEL from .env

# --- Tests ---
pytest==9.1.1              # test runner (LLM + retriever mocked, no network/key)
httpx==0.28.1             # required by fastapi.testclient.TestClient
